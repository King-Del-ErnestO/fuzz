python3.10 run_fuzzer.py


python3.10 run_fuzzer.py fuzz_tool/corpus/


python3.10 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Run with seed corpus for 60s
python run_fuzzer.py --target json --time_budget 60 fuzz_tool/corpus

# Simulated crash target (quick demo): add a file containing "CRASH!" to corpus
echo 'CRASH!' > fuzz_tool/corpus/crash_seed.txt
python run_fuzzer.py --target edge --time_budget 60 fuzz_tool/corpus


python run_fuzzer.py --target calc --time_budget 60 fuzz_tool/corpus

# Example: see 5 traced calculator evaluations, including expected failures
python run_fuzzer.py --target calc --time_budget 20 \
  --trace_calc 5 --trace_errors --continue_on_crash fuzz_tool/corpus


find . -maxdepth 1 -type f ! -name "*.txt" -exec rm -f {} +


python run_fuzzer.py --target calc --time_budget 20 \
  --trace_calc 8 --trace_errors --demo_ops --continue_on_crash \
  --summary_interval 3 \
  fuzz_tool/corpus
